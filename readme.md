# Keyclock service (Authentication Broker)

Includes PostgreSQL db

## For install and Run

Set **.env** config file

```bash
$ docker-compose up -d --build
```

## For Run
```bash
$ docker-compose up -d
```

##Locate to URL
http://your-server-ip:8087/
